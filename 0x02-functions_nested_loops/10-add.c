#include "main.h"

/**
 * add - adds two integers and returns the result
 * @s: int to be added to m
 * @m: int to be added to s
 * Return: sum of a and b
 */
int add(int s, int m)
{
	int k;

	k = s + m;

	return (k);
}
